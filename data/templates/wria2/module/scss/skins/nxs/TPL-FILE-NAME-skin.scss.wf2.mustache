// Forcing the name of the skin class
$skin-name: "nxs" !default;

// Overriding some properties
// your overrides here

// Loading the NXT sass helpers (taking precedence, before the ones from nx)
@import "../../wf2-base-css/scss/skins/nxt/wf2-sass-skin-helpers";

// Finally loading the nx skin definition
@import "../nxt/{{moduleName}}-skin.scss";

.{{moduleName}} {
  // specific code for NXS here
}
